import { View, Text } from 'react-native';

export default function Recherche() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24 }}>Recherche d'événements</Text>
    </View>
  );
}
