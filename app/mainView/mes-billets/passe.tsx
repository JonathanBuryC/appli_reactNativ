import { View, Text } from 'react-native';

export default function Passes() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>Événements passés</Text>
    </View>
  );
}
